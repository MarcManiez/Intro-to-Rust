fn get_length(string: ??) -> ?? {
    let string_length = string.len();
    return ??
}

fn main() {
    let beautiful_string = String::from("Cellar door");
    let beautiful_string_length = get_length(??);

    println!("{}, what a beautiful string, and it's {} characters long.", beautiful_string, beautiful_string_length);
}

// How do we get line 10 to print?
// What values should we pass into get_length?
// What values should we return?
